# Auditoría Git (E10)

- **Rama de entrega:** `frontend-mjtr` (iniciales MJTR — Manuel Jesús Torralbo Rodríguez).
- **Base remota:** `origin/main`; último commit `77ac387` + cambios locales sin commit.
- **Diff working tree vs HEAD:** ~9 archivos modificados (+319/-102); Kanban (`Positions.tsx`, `App.js`), tests backend, locks.
- **Sin seguimiento:** `PositionDetail/`, `hooks/usePositionKanban.ts`, `services/positionService.js`, `types/`, `frontend/prompts/`, `scripts/validate-local.sh`, `.cursor/`, `.ai/`, `AGENTS.md`, `docs/` ampliados.
- **Transcript de chat:** no en repo; trazabilidad vía `git diff` / `git log`.
