# PROJECT_SPEC

## Resumen del proyecto

**LTI (Talent Tracking System):** ATS full-stack en este repositorio — React (frontend), Express + TypeScript + Prisma (backend), PostgreSQL. Desarrollo asistido por IA con **SPEC obligatoria** antes de implementación.

## Problema

Gestión del ciclo de candidatos y posiciones para reclutamiento; detalle en [especificacion-inicial.md](./especificacion-inicial.md) y en código.

## Usuarios objetivo

**TODO:** roles finales — validar con tutor/cliente.

## Alcance MVP

- Código en `frontend/` y `backend/` según especificación y entregas E10.  
- **TODO:** delimitar MVP vs ejercicio académico.

## Fuera de alcance

- Implementación sin SPEC/tarea acotada.  
- Refactors globales o dependencias nuevas sin justificación documentada.

## Requisitos críticos pendientes

- **TODO:** historias priorizadas para la iteración actual.  
- **TODO:** criterios de aceptación por feature.

## Criterios de aceptación globales

- Implementación derivada de SPEC en `docs/` o plantillas `.ai/templates/`.  
- Tests según `TESTING_STRATEGY.md`; validación local `bash scripts/validate-local.sh` o CI (`.github/workflows/ci.yml`).  
- Decisiones relevantes en `DECISIONS.md` / ADRs.
